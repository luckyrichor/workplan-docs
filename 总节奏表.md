# 总节奏表

最后更新：2026-09-22　｜　周期：第 1 周起 13 周（2026-09-20 ～ 2026-12-19）

本文件只管**排期与工时**。各项目做什么看该仓库的 `AGENTS.md`，已发生的进展看各仓库 `docs/progress.md`，汇总看 `进度总览.md`。

---

## 一、工时账（先把算术摆清楚）

| 项 | 数值 |
|---|---|
| 每周投入 | 10–15 小时 |
| 周期 | 13 周 |
| **总预算** | **130 ～ 195 小时** |
| 本表计划总工时 | **151 小时**（主力 125 + 维持 26） |

> 2026-09-22 由 143 h 调整为 151 h：新增 ④ M7（LLM 提取器，5 h），并把三项技术债折进已有里程碑（M2 +2 h、M4 +1 h）。详见第五节。

### 赤字提示

- 按**每周 15 小时**：预算 195 h，计划 151 h —— 有 44 h 余量，宽裕。
- 按**每周 12.5 小时**（中位）：预算 162.5 h，计划 151 h —— 有 11.5 h 余量，偏紧但可行。
- 按**每周 10 小时**（下限）：预算 130 h，计划 151 h —— **赤字 21 小时（约 16%）**。

赤字比调整前（13 h）更深了，这是加活的直接代价，**如实记在这里**。用户 2026-09-22 明确表示时间不是约束，因此本表按 151 h 排；若实际投入持续接近下限，取舍仍由用户做。

**赤字由用户自行取舍，本表不预设降级方案。** 若实际投入接近下限，可能的处理方向是推迟某个里程碑或缩减某个实验的范围，但**具体砍哪个由用户决定**，Claude 不代为决定。

### 一个必须说明的范围风险

**① `tactical-shooter-ue` 分到 18 小时，而它的目标范围是「武器系统 + 技能 + 敌人 AI（行为树与感知）+ 简化多人同步 + 性能分析」。** 这个范围在 18 小时内只能做到「每项都有可运行的最小实现 + 记录」，做不到任何一项的深入打磨。

这不是排期失误，是六线并行的必然结果 —— 把 ① 加到 40 小时就得从别处砍掉 22 小时。**如实写在这里，是为了避免三个月后发现 ① 的成果撑不起面试时才意识到这个取舍。** 用户可以选择接受，也可以选择重新分配。

---

## 二、分配模型：主力档轮转

每周：

| 档位 | 项目数 | 单项工时 | 小计 |
|---|---|---|---|
| **主力** | 2 个 | 4–5 h | 8–10 h |
| **维持** | 2 个 | 1 h | 2 h |
| **休整** | 2 个 | 0 h | 0 h |
| | | **合计** | **10–12 h** |

余下 0–3 小时留给：写进度记录、设计问答复盘、以及必然会出现的意外。**不要把这段缓冲排满。**

- **主力档**：推进一个完整里程碑，有明确验收标准。
- **维持档**：1 小时，推进一小步并在 `docs/progress.md` 留一条记录。目的是不让任何一条线断掉超过三周。
- **休整档**：当周完全不动，不必内疚。

每个项目 13 周内的档位分布：

| 项目 | 主力档次数 | 主力工时 |
|---|---|---|
| ④ `agent-memory` | 7 | 36 h |
| ③ `agent-ops-platform` | 5 | 23 h |
| ⑥ `backend-cloud-labs` | 5 | 23 h |
| ① `tactical-shooter-ue` | 4 | 18 h |
| ② `coop-combat-unity` | 3 | 13 h |
| ⑤ `engine-core-labs` | 3 | 12 h |
| | **27** | **125 h** |

**W8 是唯一的例外，那周有三个主力档**（合计 15 h，顶到预算上限）—— ④ M7 必须排在 M5 基线对比评测（W9）之前，否则评测量的是规则提取器这个临时替身，而不是真正要交付的系统。

分配依据：④ 最多是因为它同时是岗位 01 的直接载体**和 ③ 的依赖服务**，必须先立住；③ 和 ⑥ 各覆盖 5–6 个岗位；①②⑤ 各覆盖 1–3 个岗位。**这是按岗位覆盖面和依赖关系排的，不代表重要性排序。**

---

## 三、13 周排期

同一周内不会让一个项目既是主力又是维持。

| 周 | 日期 | 主力 ①② | 维持 | 休整 |
|---|---|---|---|---|
| W1 | 09-20 ～ 09-26 | **④ M1** 可观测性地基 ／ **② M1** 工程+3C | ① ⑥ | ③ ⑤ |
| W2 | 09-27 ～ 10-03 | **④ M2** SDK+生命周期 ／ **① M1** 工程骨架+角色武器 | ③ ⑤ | ② ⑥ |
| W3 | 10-04 ～ 10-10 | **④ M3** Embedding worker ／ **⑥ E1** 导购+缓存一致性 | ② ⑤ | ① ③ |
| W4 | 10-11 ～ 10-17 | **③ M1** 多 Agent 任务拆解 ／ **⑤ L1** Job System | ① ④（④ 的维持档 = 接 CI） | ② ⑥ |
| W5 | 10-18 ～ 10-24 | **③ M2** 工具调用+集成④ ／ **① M2** 技能系统 GAS | ⑥ ② | ④ ⑤ |
| W6 | 10-25 ～ 10-31 | **④ M4** 混合检索 ／ **⑥ E2** 房间状态同步 | ③ ⑤ | ① ② |
| W7 | 11-01 ～ 11-07 | **③ M3** 流式+背压+取消 ／ **② M2** 战斗技能 | ① ⑥ | ④ ⑤ |
| W8 | 11-08 ～ 11-14 | **⑥ E3** C++ 规则引擎 ／ **⑤ L2** 三种分配器 ／ **④ M7** LLM 提取器 ⚠️三主力 15 h | ③ | ① ② |
| W9 | 11-15 ～ 11-21 | **④ M5** 基线对比评测 ／ **① M3** 敌人 AI | ② ⑥ | ③ ⑤ |
| W10 | 11-22 ～ 11-28 | **③ M4** 调用树可观测 ／ **⑥ E4** 简化调度器 | ① ⑤ | ② ④ |
| W11 | 11-29 ～ 12-05 | **① M4** 多人同步+性能分析 ／ **⑤ L3** 资源加载流水线 | ③ ④ | ② ⑥ |
| W12 | 12-06 ～ 12-12 | **④ M6** Context Builder ／ **② M3** 联机同步+重构记录 | ⑥ ① | ③ ⑤ |
| W13 | 12-13 ～ 12-19 | **③ M5** 评测集+岗位08 ／ **⑥ E5** CMDB | ④ ⑤ | ① ② |

**排期依据的两条硬约束**：

1. **③ 依赖 ④**：③ M2（集成记忆服务）在 W5，因此 ④ M2（SDK）必须在 W2 完成。这条链不能乱。
2. **①② 只能在 Windows 上做**：这两条线的主力周需要用户有整段时间在那台机器前（编辑器操作无法远程完成）。W2/W5/W9/W11（①）和 W1/W7/W12（②）要留意。

⚠️ **W2–W3 跨国庆假期（10-01 ～ 10-07）**。可能时间更多，也可能完全没有。如果那两周停摆，后续整体顺延，不要试图压缩补回来。

---

## 四、各项目里程碑

预估工时均为**估计值**，不是承诺。验收标准是「做完了」的判据 —— 达不到就是没做完，不要含糊过去。

### ④ `agent-memory`（36 h）

现状：M1 已完成（2026-09-20，103 测试全过、四道门全绿）。以下为 Phase 3–5 的推进。

| 里程碑 | 工时 | 验收标准 |
|---|---|---|
| **M1** 可观测性地基 | 5 h | 一次完整的写入/检索请求能在 trace 中看到全链路 span；有结构化日志与基础 metrics；**日志与 CLI 输出不含 Event 正文或记忆正文** |
| **M2** Python SDK + 生命周期补全 | **6 h** | ③ 能通过 SDK 完成 remember/get/correct/delete；supersede 与 archive 可用并有测试；**删除接口真正按 Idempotency-Key 幂等**（与 `POST /memories` 契约一致，重放同一 key 返回同一结果）；**有测试禁止绕过 `StructuredLogger` 直接用 `logging`** |
| **M3** Embedding worker | 5 h | 新建记忆后 embedding 异步生成并落库；可重建；失败进重试且不丢任务 |
| **M4** 混合检索 | **7 h** | PG 全文 + 向量 + 结构化三路候选各自可见命中情况；融合得分**可解释**；语义改写后的 query 能召回原记忆；**所有列表接口有统一的分页契约**（含已有的 `GET /memories/{id}/versions`） |
| **M5** 基线对比评测 | 4 h | 产出 No Memory / Naive Vector / 本方案三条基线的**可复现对比数字**（对应设计文档验收标准第 10 条） |
| **M6** Context Builder + Token 预算 | 4 h | 给定 token 预算能裁剪并优先保留高价值记忆；超预算时不溢出；行为可测试 |
| **M7** LLM 提取器 | 5 h | 真实模型提取替换 `CodingFailureRuleExtractor`，且**不限于 build/test 一种场景**；提取器走 `MemoryExtractor` Protocol，worker 无需改动；**候选不得扩大 scope、不得写入敏感信息**两条约束有测试；模型不可用时降级到规则提取器而非整条链路失败；**评测用固定夹具，不依赖线上模型**（排 W8，须早于 M5，否则基线评测量的是临时替身） |

**每个里程碑完成后四道门都要重跑**：`pytest`、`ruff`、`mypy`（strict）、`evals.foundation_runner`。

### ③ `agent-ops-platform`（23 h）

| 里程碑 | 工时 | 验收标准 |
|---|---|---|
| **M1** 骨架 + 多 Agent 任务拆解 | 5 h | 一个电商场景请求能被拆成多个子任务并分派；有可跑的端到端最小闭环 |
| **M2** 工具调用 + 集成 ④ | 5 h | 通过 ④ 的 SDK 读写记忆；**接口时序、失败降级、幂等三项都有测试覆盖** |
| **M3** 流式：SSE + 背压 + 取消 | 5 h | 慢消费者不拖垮全局；客户端断开能正确传播取消；有并发下的验证数据 |
| **M4** 调用树可观测 | 4 h | 一条请求散成的调用树能在 trace 中完整还原；出错时能定位到具体某一跳的工具调用 |
| **M5** 评测集 + 岗位 08 子方向 | 4 h | 评测集可复现、判定口径明确；工单/会话处理路径可跑。**08 的前端部分不做，差距在技术方案中如实列出** |

### ⑥ `backend-cloud-labs`（23 h）

五个独立实验，各自能讲完整故事。

| 实验 | 岗位 | 工时 | 验收标准 |
|---|---|---|---|
| **E1** 导购服务 + 缓存一致性 | 06 | 5 h | 能复现并解决一类缓存不一致；有降级路径；接口设计有文档 |
| **E2** 房间状态同步服务 | 13 | 5 h | 长连接下多客户端状态一致；有帧驱动的同步循环；断线重连可用 |
| **E3** C++ 规则表达式求值引擎 | 15 | 5 h | 支持表达式解析与求值；**有 benchmark 数据**（注明设备与负载） |
| **E4** 简化版调度器 | 16 | 4 h | 实现打分、装箱、抢占三种策略并能对比效果 |
| **E5** CMDB 配置项 + 关系图 + 变更审计 | 17 | 4 h | 配置项模型可查询关系图；变更留审计轨迹且可回溯 |

⚠️ 五个实验共用容器化、可观测、测试基建能省时间，但**共用基建不等于五个方向的专项能力都已覆盖**。每个实验的实际覆盖范围在其目录 README 中如实说明。

### ① `tactical-shooter-ue`（18 h）

环境已验证（UE 5.6.1 + MSVC 14.44，最小工程编译通过）。**范围与工时的矛盾见第一节。**

| 里程碑 | 工时 | 验收标准 |
|---|---|---|
| **M1** 工程骨架 + 角色与武器基础 | 5 h | 工程能编译能运行；角色可移动可射击；武器有基本状态机 |
| **M2** 技能系统（GAS） | 5 h | 至少两个技能走完整 GAS 链路（Ability + Effect + Cue） |
| **M3** 敌人 AI（行为树 + 感知） | 5 h | 敌人能感知玩家并做出巡逻/追击/攻击的状态切换 |
| **M4** 简化多人同步 + 性能分析 | 3 h | 关键状态能复制；用 Unreal Insights 抓一次 profile 并记录结论 |

分工：Claude 通过 ssh 写 C++ 并编译；用户在编辑器做场景搭建、蓝图连线、Profiler 分析与肉眼验证。

### ② `coop-combat-unity`（13 h）

环境已就绪（Unity 2022.3.38f1c1）。

| 里程碑 | 工时 | 验收标准 |
|---|---|---|
| **M1** 工程 + 3C | 5 h | **建工程时先设 Force Text 与 Visible Meta Files**；角色控制与第三人称相机手感可用 |
| **M2** 战斗技能 | 4 h | 至少两个技能有完整的输入→判定→表现链路 |
| **M3** 简化联机同步 + 重构记录 | 4 h | 双端状态一致；**重构过程与取舍有书面记录**（这是岗位 09 的考点之一） |

### ⑤ `engine-core-labs`（12 h）

**偏学习模式：Claude 写脚手架与测量框架，每个实验最核心的算法留空由用户实现。不要因为「顺手就写了」而补上。**

| 实验 | 工时 | 用户手写部分 | 验收标准 |
|---|---|---|---|
| **L1** Job System（work stealing） | 5 h | `steal()`（约 30–50 行） | 产出线程数与吞吐的关系曲线，注明设备与负载 |
| **L2** 三种分配器（pool/stack/arena） | 4 h | arena 的分配与回收 | 产出分配延迟分布与碎片率对比数据 |
| **L3** 资源加载流水线 | 4 h | 异步加载的调度逻辑 | 产出同步/异步/预加载三种方式的帧时间尖峰对比 |

C++ 环境：Mac 未装 cmake，**建议在 tx 上做**（gcc 13 + cmake 3.28 齐全）。

---

## 五、技术债与已知缺口（2026-09-22 评审）

外部评审对 ④ 提出五点，逐条对着代码核实过，**五条全部属实**。处置如下 —— 每一条要么有归属，要么明确不做，**不留"以后再说"**。

| # | 缺口 | 处置 | 落在哪 |
|---|---|---|---|
| 1a | 检索只能按 `memory_id` 精确查，无向量/全文/混合 | 做 | 已有排期：M3（W3）→ M4（W6）→ M6（W12） |
| 1b | 提取器只认 build/test 硬编码规则 | 做 | **新增 M7**（W8） |
| 2 | `GET /memories/{id}/versions` 无分页 | 做，但不单独做 | 折进 **M4**（W6），与新增的列表接口一次定契约 |
| 3 | 没有 CI，四道门只在本地手动跑 | 做，**最先做** | ④ 在 **W4** 的维持档（1 h，用已有预算） |
| 4 | 删除接口的 `Idempotency-Key` 是摆设 | 做 | 折进 **M2**（W2） |
| 5 | 遥测字段白名单的 64 字符上限 | **不改代码** | 见下 |

**第 5 条为什么不改**：上限管的是字段**值**不是字段名，而现存最长的 reason code 是 `AUTOMATIC_MEMORY_PIPELINE_FAILED`（32 字符），离上限还有一倍空间，不构成实际约束。`docs/design-decisions.md` 里"长的 reason code 也会被拒"那句把摩擦说重了，**要改的是那句措辞**。"加字段要改全局文件"这个机制保留 —— 那是目的不是副作用。

**评审没提到、但一并记下的两项**（暂不排期，如实写进 README 边界）：

- **物理删除没有执行者**：`DeletionRequest` 只落库，接口却返回 `pending_physical_cleanup`。比分页严重。
- **没有限流**：设计文档第 5 节的 API Gateway 列了 Rate Limit，实现里没有。

两项都属于"明确知道、暂时不做"，**不得在任何地方声称已支持**。

---

## 六、怎么用这张表

1. **每周开始时**看本周的主力与维持，到对应仓库的 `AGENTS.md` 确认要做什么。
2. **每次动手后**在该仓库 `docs/progress.md` 记一条：日期、做了什么、怎么验证的、结果如何。**失败和返工也要记。**
3. **每周结束时**更新 `进度总览.md` 的看板与累计工时。
4. **实际工时与预估差得多时，改这张表，不要假装没差。** 预估本来就是估的，改它不丢人，拿着一张早就不准的表往下走才有问题。

### 排期变更原则

- 里程碑可以推迟，**不要压缩验收标准**。「做完了」的判据一旦松动，整张表就失去意义。
- 某条线连续三周没动，在 `进度总览.md` 的风险区标出来。
- **不因为「时间可能不够」提前砍掉 ⑤ 和 ⑥**。取舍由用户在真的不够时做。
