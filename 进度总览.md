# 进度总览

最后更新：2026-09-20　｜　当前：**第 1 周**（2026-09-20 ～ 09-26）

汇总看板。**详细进展写在各仓库的 `docs/progress.md`，这里只做汇总**，不要在本文件展开细节。排期见 `总节奏表.md`。

---

## 本周（W1）

| 档位 | 项目 | 里程碑 | 预估 |
|---|---|---|---|
| **主力** | ④ `agent-memory` | M1 可观测性地基 | 5 h |
| **主力** | ② `coop-combat-unity` | M1 工程 + 3C | 5 h |
| 维持 | ① `tactical-shooter-ue` | 推进一小步并留记录 | 1 h |
| 维持 | ⑥ `backend-cloud-labs` | 同上 | 1 h |
| 休整 | ③ `agent-ops-platform` | — | 0 |
| 休整 | ⑤ `engine-core-labs` | — | 0 |

② 的主力周需要用户在 Windows 机器前操作编辑器；**建工程时先设 Force Text 与 Visible Meta Files**，否则 `.prefab`/`.unity` 是二进制，无法 diff 也无法合并。

---

## 看板

| 项目 | 状态 | 当前里程碑 | 已完成 | 累计工时 | 最近更新 |
|---|---|---|---|---|---|
| ④ `agent-memory` | **进行中** | M1 可观测性地基 | 0 / 6 | 0 h | 2026-09-20 基线验证 |
| ③ `agent-ops-platform` | 未开工 | — | 0 / 5 | 0 h | — |
| ⑥ `backend-cloud-labs` | 未开工 | — | 0 / 5 | 0 h | — |
| ① `tactical-shooter-ue` | 未开工 | — | 0 / 4 | 0 h | 2026-09-20 工具链验证 |
| ② `coop-combat-unity` | **本周开工** | M1 工程 + 3C | 0 / 3 | 0 h | — |
| ⑤ `engine-core-labs` | 未开工 | — | 0 / 3 | 0 h | — |

**累计工时 0 / 计划 143 h**（预算 130–195 h）

---

## 环境就绪情况

六条线的环境**全部就绪**，无待办。

| 项目 | 机器 | 环境状态 |
|---|---|---|
| ④ ③ ⑥ | tx（推荐）／ Mac ／ Windows | ✅ Postgres 常驻 tx；④ 基线 75 测试全过、四道门全绿 |
| ⑤ | tx（推荐，gcc13 + cmake3.28） | ✅ Mac 未装 cmake，建议在 tx 做 |
| ① | luowindows | ✅ UE 5.6.1 + MSVC 14.44，最小工程实编通过（82s 首编 / 38s 重编） |
| ② | luowindows | ✅ Unity 2022.3.38f1c1 |

---

## 风险与阻塞

**阻塞项：无。**

已知风险（不阻塞，但要盯着）：

| 风险 | 说明 | 何时会变成问题 |
|---|---|---|
| **① 范围与工时不匹配** | 18 h 要覆盖武器、技能、AI、同步、性能分析，只能做到「每项最小实现」。见 `总节奏表.md` 第一节 | W11（① M4）结束时，如果成果撑不起面试叙述 |
| **每周 10 h 下限时有 13 h 赤字** | 计划 143 h，下限预算 130 h。取舍由用户做，本表不预设降级 | 实际投入连续几周接近 10 h 时 |
| **luowindows 内存 13.7 G** | UE 官方推荐 32 G。已把编译并行度限到 12。编译报内存错误时先调这个值，别先怀疑代码 | ① 自有代码积累变多后 |
| **tx 内存 7.5 G** | Postgres + ④ API + ③ 服务同时跑要留意；起新服务前先 `free -h` | W5 之后 ③ 与 ④ 同时常驻时 |
| **tx `ulimit -n` 仅 1024** | ③ M3 的高并发流式压测可能撞上限 | W7（③ M3） |
| **W2–W3 跨国庆** | 10-01 ～ 10-07。可能时间更多，也可能停摆 | 如果停摆，后续顺延，不要压缩补回来 |

---

## 待补充材料

- **UnityGames** 参考项目用户尚未提供。**在实际读到之前，不得假设或杜撰其架构、内容或带做节奏。**

---

## 更新方式

**每周结束时**更新本文件：

1. 把「本周」换成下一周（照 `总节奏表.md` 抄）
2. 更新看板的状态、已完成数、累计工时、最近更新日期
3. 累计工时要**填实际值**，和计划差得多就在 `总节奏表.md` 里改预估，不要两边都装作没差
4. 某条线连续三周没动，写进风险区
5. 新出现的阻塞项写进「风险与阻塞」，解决后移除并在对应仓库 `docs/progress.md` 留记录

**本文件只做汇总。** 具体做了什么、怎么验证的、踩了什么坑，写在各仓库 `docs/progress.md`；环境与工具链的坑写在 `环境与踩坑记录.md`；设计取舍写在各仓库 `docs/design-decisions.md`。
