# 环境与踩坑记录

最后更新：2026-09-19

三台机器的实际状态、各自的注意事项、以及已经踩过的坑。**动环境之前先读这份。**

> **标注约定**：本文档区分两类内容 ——
> **［实测］** 本机实际验证过的事实，附验证方式。
> **［预警］** 已知的常见风险，本项目尚未实际触发，属于提前提醒，**不要当成已确认的故障**。

---

## 一、三台机器速查

| | Mac | tx | luowindows |
|---|---|---|---|
| 系统 | macOS（APFS） | Ubuntu 24.04 | Windows 11 家庭版 |
| 工作区 | `/Users/lc/Project/WorkPlan` | `/home/ubuntu/project/WorkPlan` | `E:\WorkPlan` |
| 登录用户 | lc | ubuntu | shnh2 |
| CPU / 内存 | — | 4 核 / 7.5G（swap 1.9G） | 32 核 / **13.7G** |
| 磁盘可用 | 259G | 103G | C138 / D203 / **E153** / F69 G |
| 文件名大小写 | **不敏感** | **敏感** | **不敏感** |
| GitHub | ssh ✅ 读写 | ssh ✅ 读写 | ssh ✅ 读写 |
| 定位 | 日常开发、写文档 | 常开，适合跑常驻服务 | **引擎唯一机器** |

三台 git 身份统一 `luckyrichor / luckyrichor@gmail.com`，七个仓库 remote 全部 `git@github.com:luckyrichor/<repo>.git`。

---

## 二、三台的代理机制完全不同

**最容易搞混的地方：三台都装了 Mihomo，但生效方式各不相同，不能拿一台的经验套另一台。**

| | 机制 | ssh 是否走代理 | 判断方式 |
|---|---|---|---|
| Mac | **TUN 全局接管** | ✅ 走 | `netstat -rn` 有 `198.18.0.1 → utun5` |
| tx | **纯端口模式 + 环境变量** | ❌ 不走（真直连） | 配置无 `tun:` 段，只有 `mixed-port: 7890` |
| Windows | **TUN 全局接管**（依赖服务模式） | ✅ 走 | `Get-NetRoute` 里 fake-IP 池有路由 |

### tx 的特殊之处

`~/.config/mihomo/config.yaml` **没有 `tun:` 段**，系统 DNS 指向腾讯云 `183.60.83.19`，配置里那段 `dns: enhanced-mode: fake-ip` 是**死的**（没人查 mihomo 的 DNS）。代理靠 `~/.bashrc` 里默认调用的 `proxy_on()` 设置环境变量。

［实测］非交互执行时的出网能力（`ssh tx '...'`，无代理变量）：

```
github.com HTTPS         失败        ← 要代理
pypi.org / npmjs / 腾讯镜像  HTTP 200   ← 直连可达
git over ssh             成功        ← 我们用的方式
```

**结论**：日常够用。只有从 GitHub 走 HTTPS 拉东西（release 包、raw 文件）时要显式带代理变量。**不建议给 tx 加 TUN**，多一层故障源。

---

## 三、按机器分列的注意事项

### Mac

［实测］
- **文件系统大小写不敏感**（`a.txt` 与 `A.txt` 视为同一文件）。
- **未装** cmake、git-lfs、dotnet。系统 python 是 3.9.6，但 `uv` 在，各项目自带 3.12 环境，不受影响。
- **Docker daemon 默认没启动**。`agent-memory` 的 `tests/api`/`integration`/`e2e` 走 Testcontainers，跑之前要先开 Docker Desktop。
- `ulimit -n` = 1048576，宽裕。

［预警］
- ⑤ `engine-core-labs` 是 C++/CMake，Mac 上要用得先装 cmake；**也可以直接在 tx 上做**（那边 gcc13 + cmake3.28 齐全），省得污染本机。
- 引擎项目（①②）不在这台机器上开发，不需要装 git-lfs。若哪天 clone 了引擎仓库，LFS 文件会以指针形式存在，**不要以为文件损坏了**。

### tx（Ubuntu 服务器）

［实测］
- **`ubuntu` 已加入 `docker` 组**（2026-09-19，用户同意后执行）。docker 命令免 sudo，Testcontainers 可直接用。
  - 当时评估：该用户本来就有**免密 sudo**，已是 root 等价，加 docker 组**不新增任何权限**，所以通常说的「docker 组 = root 级权限」这条安全权衡在本机不成立。
- **文件系统大小写敏感**（三台里唯一一台）。
- **`Linger=yes`** —— 用户级 systemd 服务在退出登录后仍会运行。适合把 ④ 的记忆 API 常驻在这里供 ③ 调用。
- 时区 `Asia/Shanghai`，与本地一致，日志时间戳不用换算。
- `ulimit -n` = **1024**，偏低。
- **PATH 的坑（2026-09-19 已修）**：`/etc/profile.d/devtools.sh` 只在**登录 shell** 加载，`ssh tx 'cmd'` 这种非交互执行**读不到**，`go` 和 `uv` 会 `command not found`。
  已把 `go` / `gofmt` / `uv` / `uvx` 软链到 `/usr/local/bin`，现在非交互也能直接用，不必每条命令都套 `bash -lc`。
  `node` / `cmake` / `docker` / `git` 本来就在默认 PATH 上，不受影响。

［预警］
- **内存 7.5G 是这台机器的主要约束**。Postgres + ④ 的 API + ③ 的服务同时跑要留意；真跑起来先看 `free -h` 再加服务。
- **`ulimit -n` 1024 可能在 ③ 的高并发流式压测时成为瓶颈**（每条 SSE/WebSocket 连接占一个 fd）。压测前先调 `ulimit -n` 或改 systemd 单元的 `LimitNOFILE`。
- 磁盘 103G 充裕，但 Docker 镜像和 Postgres 数据会累积，定期 `docker system df` 看一眼。

### luowindows（引擎机）

［实测］
- **Unity 装在 `F:\Unity_Hub`**（Hub 改过安装路径，别去默认的 `C:\Program Files\Unity\Hub\Editor` 找）。三个版本：`2022.3.38f1c1`、`6000.3.23f1`、`6000.6.0f1`。
- **UE 尚未安装；已确定安装 `5.6`**（与本机 VS 2022 17.14 为官方推荐配对）。装到 **D 盘**，不要装 E 盘。
- **`LongPathsEnabled = 1`**（Windows 层面已启用）。
- **`core.longpaths` / `core.fscache` 已设为 true**（2026-09-19 设置）。Git 的长路径限制和 Windows 的是**两套独立开关**，只开系统那个不够。
- 页面文件 `C:\pagefile.sys`，上限 20480 MB。
- 已装：git2.55、git-lfs3.8、dotnet、python3.14、node。**未装 cmake、gh**。
- **Visual Studio 有三套，UE 必须用 2022 那套**［实测 2026-09-19，已验证 devenv.exe 真实存在］：

  | 实例 | 路径 | MSVC | C++ 游戏开发工作负载 |
  |---|---|---|---|
  | **Community 2022** | **`F:\VS\IDE`**（非常规位置，不在 C 盘） | **14.44.35207** | ✅ 唯一装了的 |
  | Community 2026 | `C:\Program Files\Microsoft Visual Studio\18\Community` | 14.51.36231 | ❌ |
  | 生成工具 2022 | `C:\Program Files (x86)\...\BuildTools` | 14.44.35207 | ❌（无 IDE 属正常） |

  **UE 与 VS 的兼容性详见 `tactical-shooter-ue/AGENTS.md`**（已查证官方表格）。要点：本机 VS 2022 17.14 可编译 UE 5.6/5.7/5.8，UE 所需工作负载与组件均已具备，装 UE 前不用动 VS。两个 VS 并存时需在 `BuildConfiguration.xml` 锁定 `<Compiler>VisualStudio2022</Compiler>`。
- `.ssh/config` 里 `Host github.com` 用 luckyrichor 密钥；老的 shnh2001 密钥保留为 `Host github-shnh2001` 别名。改动前备份在 `~/.ssh/config.bak-before-luckyrichor`。
- 参考项目 `E:\Unity_project\unity-learning-journal` 的 `m_SerializationMode: 2`，即 **Force Text**，做法正确，新建 Unity 工程照抄。

［预警］
- **UE 建议装 D 盘**（203G 空闲），不要装 E 盘 —— E 盘要留给工作区，UE 单版本加 DerivedDataCache 轻松吃掉 100G+。
- **13.7G 内存编 UE C++ 会吃紧**。32 核并行编译时内存会先于 CPU 成为瓶颈，可能要在 `BuildConfiguration.xml` 里限制 `MaxParallelActions`。编译失败先看是不是内存不够，别急着怀疑代码。
- **Defender 是完全关闭的，不要试图给它加排除目录**［实测 2026-09-19］：`RealTimeProtectionEnabled=False`、`AntivirusEnabled=False`、`AMServiceEnabled=False`（服务本身都没跑）。被第三方杀软接管后，Defender 的排除项设置界面会隐藏，加了也无效。
  - **实际在跑的只有火绒一个**：`HipsDaemon`（228MB）/ `HipsTray` / `wsctrlsvc`，服务 `HipsDaemon` + `HRWSCCtrl`。火绒在安全中心注册的是「Windows 安全中心控制器」而非杀毒软件，所以列表里看不到它。
  - 安全中心里那条「腾讯电脑管家系统防护」是**卸载未清干净的僵尸注册**：`F:\Tencent_diannao_guanjia\` 目录不存在、进程没有、卸载注册表项没有。纯显示问题，不占资源，不用管。另有一个 `QPCore`（QQProtect）服务处于 Stopped，是跟 QQ/微信装的，也不影响。
  - 真要加排除目录，地方是**火绒的「信任区」**，不是 Defender。**但先不要加**——UE 装完实测一次完整编译耗时，有基线再决定值不值。
- **新建 Unity 工程必须先设两项**：`Editor > Asset Serialization` → **Force Text**，`Version Control Mode` → **Visible Meta Files**。否则 `.prefab`/`.unity` 是二进制，无法 diff 也无法合并。
- 新工程的 `m_LineEndingsForNewScripts` 建议设为 Unix，与仓库 `.gitattributes` 的 `eol=lf` 一致（参考项目当前是 `0` = OS Native）。
- **引擎仓库必须 `git lfs install`**（两个引擎仓库已配置 `.gitattributes` 的 LFS 规则并已执行过）。

### 跨机器的共同风险

［实测］**文件名大小写在三台之间不一致**：tx 敏感，Mac 与 Windows 不敏感。

［预警］由此产生的两个具体风险：
- 在 tx 上创建仅大小写不同的两个文件（如 `Foo.cs` 与 `foo.cs`），同步到另外两台会**撞成一个文件**。
- 只改大小写的重命名（`git mv foo.cs Foo.cs`）在 Mac/Windows 上**不会被 git 识别**。必须分两步：先改成临时名提交，再改成目标名提交。

---

## 四、已踩过的坑

每条标注适用机器。**这些都是本项目实际发生过的**，不是理论风险。

### 1. 非交互 ssh 不加载 `.bashrc` ｜ tx

`ssh tx 'env | grep proxy'` 看不到任何代理变量，会让人误判「代理没在用」。Ubuntu 的 `.bashrc` 开头有：

```bash
case $- in *i*) ;; *) return;; esac
```

非交互 shell 直接 return，后面的 `proxy_on()` 根本不执行。`bash -lc` 也一样（登录 shell 但非交互）。

**看真实交互环境用 `bash -ic`**：`ssh tx 'bash -ic "env | grep proxy"'`
**反过来**：远程跑命令时默认没有代理，需要代理的命令要显式 `export`。

### 2. PowerShell 5.1 的 BOM ｜ Windows —— 本次踩了三次，代价最大

两条**方向相反**的规则，极易记混：

| 操作 | 规则 |
|---|---|
| **传 `.ps1` 给 Windows 执行** | **必须带 UTF-8 BOM**。PS 5.1 默认按 ANSI/GBK 读脚本，无 BOM 的中文会乱码并导致解析错误 |
| **在 Windows 上写文件** | **绝不能带 BOM**。`Set-Content -Encoding UTF8` 和 `Out-File -Encoding UTF8` **都会写 BOM** |

写文件的正确姿势：

```powershell
[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))
```

生成带 BOM 的脚本：`printf '\xEF\xBB\xBF' > out.ps1 && cat body.ps1 >> out.ps1`

三次实际故障：

1. **写 `~/.ssh/config` 带了 BOM** → Git 自带的 MinGW 版 ssh 报 `Bad configuration option: ﻿host`，所有 git 操作失败。**而 Windows 原生 `ssh.exe` 却容忍 BOM**，造成「`ssh -T` 成功但 `git ls-remote` 失败」的迷惑现象，排查时极易误判。
2. **写 commit message 文件带了 BOM** → 提交标题混进不可见字符（`﻿refactor: ...`），要 `--amend` + `--force-with-lease` 才能修。
3. **仓库里的 `bootstrap-workspace.ps1` 自己没有 BOM** → 在 Windows 实跑直接报 `The string is missing the terminator`。规则已经写进文档，却没应用到脚本自身。现已加 BOM 并在文件头写了注释警告。

### 3. 别用裸 TcpClient 测 fake-IP 的连通性 ｜ Windows

TUN 环境下 `New-Object System.Net.Sockets.TcpClient` 连 `198.18.x.x` 会给出误导性的「不通」。**测连通性要用实际协议**（`Invoke-WebRequest`、`curl`），或直接看路由表。

本次因此误判「Windows 连不上 GitHub 22 端口，所以 SSH 不可用」，进而错误断言「SSH 没法走 HTTP 代理，必须改用 HTTPS」。**实际上 TUN 工作在网络层，SSH 和 HTTPS 一视同仁**，根本不存在这个区别 —— 真正的问题是 TUN 没生效。

### 4. Clash Verge 的 TUN 依赖「服务模式」｜ Windows

装路由需要管理员权限，靠一个后台服务完成。**服务模式没启用时，TUN 网卡能建起来但路由装不上**，结果是 fake-IP DNS 生效、流量无处可去 —— 整机所有域名访问全断（连 baidu 都不通），极易误判成「断网了」。

判断：`Get-NetRoute -AddressFamily IPv4 | ? DestinationPrefix -like "198.18*"`。只有 `/30`、`/32` 那几条（网卡自己的小网段）= **没接管**。

修复：Clash Verge → 设置 → 系统设置 → **服务模式**启用（弹 UAC），然后 TUN 关掉再打开。

### 5. 移动目录会让 Python venv 失效 ｜ 全平台

venv 里的 shebang 和 `pyvenv.cfg` 写的是**绝对路径**。`agent-memory` 从 `MemorySystem/Codex` 搬到 `WorkPlan/` 之后，`.venv/bin/mypy` 直接报 `bad interpreter`。

**搬完目录先 `rm -rf .venv && uv sync --all-groups`**，不要试图修补。

### 6. Windows 的 ssh 私钥必须收紧权限 ｜ Windows

OpenSSH 拒绝加载权限过宽的私钥。放进 `~/.ssh/` 后必须：

```powershell
icacls $key /inheritance:r /grant:r "$($env:USERNAME):R"
```

### 7. `github.com` 这个 ssh 入口只能绑一把密钥 ｜ Windows

luowindows 上原本绑的是 shnh2001 的密钥，导致 ssh 认成 shnh2001。改默认之前**先扫一遍那台机器上有没有别账号的仓库**（本次扫描：零个 shnh2001 仓库，所以直接改默认最干净）。改动前备份配置文件。

### 8. 读配置文件别用 `head` 截断 ｜ 全平台

第一次读 luowindows 的 `.ssh/config` 用了 `head -40`，正好把末尾已有的 `Host github.com` 截掉，误判「没有 github 配置」。**读配置文件要读全。**

### 9. 远程输出的两个干扰源 ｜ tx / Windows

- **tx**：腾讯云登录会打印一大幅 ASCII 二维码（微信扫码提示）。批量命令要用标记 + `sed -n '/=== MARK ===/,$p'` 过滤，否则输出全是噪音。
- **Windows**：**PowerShell 脚本的输出是跑完才一次性返回的**，中途看起来像卡死，其实只是在跑慢测试。别急着重试，那会留下一堆挂起的 ssh 会话。

### 10. `/etc/profile.d` 只在登录 shell 加载 ｜ tx

与坑 1 是同一类但不同机制：`.bashrc` 卡在「非交互」，`/etc/profile.d/*.sh` 卡在「非登录」。

`ssh tx 'uv --version'` 报 `command not found`，而 `ssh tx 'bash -lc "uv --version"'` 正常 —— 因为 `uv` 在 `~/.local/bin`、`go` 在 `/usr/local/go/bin`，两者都靠 profile.d 加进 PATH。

**已通过软链到 `/usr/local/bin` 根治**，不再需要每条命令套 `bash -lc`。以后在 tx 上装新工具，如果装到非标准目录，记得一并软链，否则远程自动化会再踩。

### 11. 给杀软加排除目录前，先确认哪个杀软在管事 ｜ Windows

一开始直接按 Windows Defender 的路径给了加排除目录的操作步骤，用户照着找不到对应设置项——因为 **Defender 已被第三方杀软接管并完全停用**，设置界面会隐藏。

其实探测时已经有信号：`(Get-MpPreference).DisableRealtimeMonitoring` 返回**空值**，当时标了句「状态不明」就放过去了。**空值本身就是征兆，应该追下去。**

**而且安全中心的注册列表本身也不可信**：本机列出的「腾讯电脑管家系统防护」其实早已卸载，目录和进程都不存在，只剩一条僵尸注册。只看列表会得出「两套杀软在跑」的错误结论（我就得出过）。

判断哪个杀软在实际工作，三步都要做——列注册、查 Defender 真实状态、**验证 exe 是否真的存在**：

```powershell
Get-CimInstance -Namespace root/SecurityCenter2 -ClassName AntiVirusProduct |
  Select displayName, pathToSignedProductExe,
         @{n='exe存在';e={Test-Path $_.pathToSignedProductExe}}
(Get-MpComputerStatus) | Select RealTimeProtectionEnabled, AntivirusEnabled, AMServiceEnabled
Get-Process | Where-Object { $_.ProcessName -match 'Hips|QQPCMgr|360|Huorong' }
```

### 12. `usermod -aG` 之后当前会话看不到新组 ｜ tx

`sudo usermod -aG docker ubuntu` 执行成功，但同一个 ssh 会话里 `id -nG` 依然没有 `docker`，`docker ps` 照样拒绝。**不是命令失败** —— 组成员身份在登录时写进进程凭据，之后不会刷新。

**必须新开一个会话才生效**，验证也要在新会话里做，否则会误判成没加上。

### 13. 跨平台比对字符串前，先剥掉 Windows 的 `\r` ｜ 全平台

做七仓库四方（GitHub / Mac / tx / Windows）哈希审计时，四列值肉眼**完全相同**，脚本却把 7 个仓库全判成不一致。原因是 Windows 侧返回的每行结尾带 CR：

```
w o r k p l a n - d o c s | 6 f 5 c 0 b 6 d \r \n
```

`"6f5c0b6d\r" != "6f5c0b6d"`。**是比较逻辑错了，不是数据不同步。**

凡是把 Windows 的输出拿去和 Mac/Linux 的做字符串比较（哈希、版本号、路径、文件名），都要先清洗：

```bash
tr -d '\r' < win.txt | sed 's/[[:space:]]*$//' > win_clean.txt
```

这类 bug 的危险在于**结论方向是"全都不一致"**——看起来像出了大事，容易让人跑去"修"本来没坏的东西。看到「全部失败」而不是「部分失败」时，先怀疑比较逻辑。

### 14. 审计脚本里不要吞掉 git 的错误输出 ｜ 全平台

批量 pull 七个仓库时写了 `git -C $p pull -q --ff-only 2>$null`，其中一个仓库因瞬时网络抖动没拉成，**错误被 `2>$null` 吞掉，输出看起来一切正常**。只因为后面逐个比对了提交哈希才发现它落后一个提交。

**多仓库批量操作要么保留错误输出，要么把「比对哈希」作为最终判据**，不要只看命令有没有报错。

［实测］Windows 上已出现过**两次**偶发 `git pull` 失败（`workplan-docs` 与 `engine-core-labs` 各一次），重跑即 `Already up to date`。推测与 TUN 代理的瞬时抖动有关。**偶发失败是常态，所以哈希比对不是可选项。**

### 15. Testcontainers 需要 Docker daemon ｜ 全平台

`agent-memory` 的 `tests/api`、`tests/integration`、`tests/e2e` 走 Testcontainers。Docker 不可用时直接抛 `DockerException` 而**不是 skip**，看起来像代码坏了。

两种不可用要分清：daemon 没启动（Mac 上常见）、或**有 daemon 但无权访问 socket**（报 `PermissionError(13)`，tx 加入 docker 组之前就是这个）。

---

## 五、Docker 运行位置（用户明确要求）

**需要 Docker 时默认跑在 `tx` 上**，不要在 Mac 或 Windows 起容器；确有必要在本地跑，**必须先征得用户同意**。

tx 上已就绪：`agent-memory` 的 Postgres（`pgvector/pgvector:pg16`，容器 `agent-memory-postgres-1`，端口 55432，带 healthcheck，数据存在 named volume 里，`docker compose down` 不会丢）。

`ubuntu` 已在 docker 组，**命令不需要 `sudo`**。

## 六、换机器工作的固定动作

1. **开工前 `git pull`**
2. 干活
3. **收工前 `git push`**，不留未推送的提交过夜

**同一个项目同一时间只在一台机器上改。** 六个项目本就独立，不存在必须同机协作的场景。

新机器拉齐工作区：

```bash
bash workplan-docs/scripts/bootstrap-workspace.sh          # Mac / Linux
powershell -ExecutionPolicy Bypass -File workplan-docs\scripts\bootstrap-workspace.ps1   # Windows
```

首次还需要：配 git 身份、把该机器 ssh 公钥加到 GitHub（`ssh -T git@github.com` 应回 `Hi luckyrichor!`）、引擎项目 `git lfs install`。
