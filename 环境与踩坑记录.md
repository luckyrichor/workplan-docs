# 环境与踩坑记录

最后更新：2026-09-19

多机环境的实际状态，以及已经踩过、不要再踩的坑。**动环境之前先读这份。**

---

## 一、三台机器速查

| | Mac | tx | luowindows |
|---|---|---|---|
| 系统 | macOS | Ubuntu 24.04 | Windows 11 家庭版 |
| 工作区 | `/Users/lc/Project/WorkPlan` | `/home/ubuntu/project/WorkPlan` | `E:\WorkPlan` |
| 登录用户 | lc | ubuntu | shnh2 |
| 规格 | — | 4 核 / 7.5G / 103G 可用 | 32 核 / **13.7G** / C138 D203 E153 F69 GB 可用 |
| GitHub | ssh ✅ 读写 | ssh ✅ 读写 | ssh ✅ 读写 |
| 备注 | — | 常开，适合跑常驻服务 | 引擎唯一机器 |

三台的 git 身份统一 `luckyrichor / luckyrichor@gmail.com`，七个仓库 remote 全是 `git@github.com:luckyrichor/<repo>.git`。

### 各机器已装工具

- **Mac**：python3.9、go、node22、docker、git、uv、gh（已登录 luckyrichor）。无 cmake、无 git-lfs、无 .NET、无引擎
- **tx**：git、docker、python3.12、gcc13、cmake3.28、go1.27、node22、uv、gh（已登录）。PATH 由 `/etc/profile.d/devtools.sh` 提供
- **luowindows**：git2.55、git-lfs3.8、VS Community 2022+2026+生成工具2022、dotnet、python3.14、node。**无 cmake、无 gh**
  - **Unity 装在 `F:\Unity_Hub`**（Hub 改过安装路径，别去默认目录找）：`2022.3.38f1c1`、`6000.3.23f1`、`6000.6.0f1`
  - **UE 尚未安装**。装的时候建议放 **D 盘**（203G 空闲），E 盘要留给工作区；UE 单版本加 DDC 轻松 100G+
  - 13.7G 内存编 UE C++ 会吃紧，32 核并行编译时可能需要限制并行度

---

## 二、三台机器的代理机制完全不同

这是最容易搞混的地方。**三台都装了 Mihomo，但生效方式各不相同**，不能拿一台的经验套另一台。

### Mac —— TUN 模式，全局接管

`github.com` 解析为 fake-IP（`198.18.x.x`），且存在 `198.18.0.1 → utun5` 的路由，流量被 TUN 网卡接走。**所有程序（含 ssh）透明走代理**，不需要任何额外配置。

### tx —— 纯端口模式 + 环境变量

`~/.config/mihomo/config.yaml` 里 **没有 `tun:` 段**，只有 `mixed-port: 7890`。系统 DNS 指向腾讯云 `183.60.83.19`，配置里那段 `dns: enhanced-mode: fake-ip` 是**死的**（没人查 mihomo 的 DNS）。

代理靠 `~/.bashrc` 里的 `proxy_on()` 设置 `http_proxy` / `https_proxy` / `all_proxy` 环境变量，**默认调用**。所以：

| 场景 | 是否走代理 |
|---|---|
| 交互式 shell 里的 curl / pip / npm | ✅ 走（出口 IP 是新加坡节点） |
| **ssh**（含 git over ssh） | ❌ **不走** —— ssh 根本不读 `http_proxy`，是真直连 |
| **非交互 ssh 命令**（`ssh tx '...'`） | ❌ **不走** —— 见下方坑 1 |

非交互下的实测出网能力：`github.com` HTTPS **失败**；`pypi.org`、`registry.npmjs.org`、`mirrors.tencent.com` 均 **200**；git over ssh **成功**。

**结论**：日常够用（remote 是 ssh、依赖源直连可达）。只有从 GitHub 走 HTTPS 拉东西（release 包、raw 文件）时要显式带代理变量。**不建议给 tx 加 TUN**，多一层故障源。

### luowindows —— TUN 模式，但依赖「服务模式」

Clash Verge 的 TUN 要装路由需要管理员权限，靠一个后台服务完成。**服务模式没启用时，TUN 网卡能建起来但路由装不上**，结果是 fake-IP DNS 生效、流量无处可去 —— 整机所有域名访问全断（连 baidu 都不通）。

判断是否生效：`Get-NetRoute -AddressFamily IPv4 | ? DestinationPrefix -like "198.18*"`。只有 `/30`、`/32` 那几条（Mihomo 网卡自己的小网段）说明**没接管**；正常时 fake-IP 池会有路由。

修复：Clash Verge → 设置 → 系统设置 → **服务模式** 启用（弹 UAC），然后 TUN 关掉再打开。

---

## 三、踩过的坑

### 1. 非交互 ssh 不加载 `.bashrc`

`ssh tx 'env | grep proxy'` 看不到任何代理变量，会让人误判「代理没在用」。原因是 Ubuntu 的 `.bashrc` 开头有：

```bash
case $- in *i*) ;; *) return;; esac
```

非交互 shell 直接 return，后面的 `proxy_on()` 根本不执行。`bash -lc` 也一样（登录 shell 但非交互）。

**要看真实交互环境用 `bash -ic`**：`ssh tx 'bash -ic "env | grep proxy"'`

反过来说：**我远程跑命令时默认是没有代理的**，需要代理的命令要显式 `export`。

### 2. PowerShell 5.1 的 BOM 问题（本次踩了两次，最坑）

方向相反的两条规则，很容易记混：

| 操作 | 规则 |
|---|---|
| **传 `.ps1` 给 Windows 执行** | **必须带 UTF-8 BOM**。PS 5.1 默认按 ANSI/GBK 读脚本文件，无 BOM 的中文会乱码并导致解析错误（`Unexpected token`、`missing terminator`） |
| **在 Windows 上写文件** | **绝不能带 BOM**。`Set-Content -Encoding UTF8` 和 `Out-File -Encoding UTF8` **都会写 BOM** |

写文件的正确姿势：

```powershell
[System.IO.File]::WriteAllText($path, $text, (New-Object System.Text.UTF8Encoding($false)))
```

带 BOM 写文件造成过的两次实际故障：

- **写 `~/.ssh/config`** → Git 自带的 MinGW 版 ssh 报 `Bad configuration option: ﻿host`，所有 git 操作失败。**而 Windows 原生 `ssh.exe` 却能容忍 BOM**，导致 `ssh -T git@github.com` 显示成功、`git ls-remote` 却失败，极具迷惑性
- **写 commit message 文件** → 提交标题里混进不可见 BOM（`﻿refactor: ...`），要 `--amend` + `--force-with-lease` 才能修

生成脚本时的做法：`printf '\xEF\xBB\xBF' > out.ps1 && cat body.ps1 >> out.ps1`

### 3. 别用裸 TcpClient 测 fake-IP 的连通性

TUN 环境下 `New-Object System.Net.Sockets.TcpClient` 连 `198.18.x.x` 会给出误导性的「不通」。**测连通性要用实际协议**（`Invoke-WebRequest`、`curl`），或者直接看路由表。

本次因此一度误判「Windows 连不上 GitHub 的 22 端口，所以 SSH 不可用」，进而错误地断言「SSH 没法走 HTTP 代理，必须用 HTTPS」。实际上 **TUN 工作在网络层，SSH 和 HTTPS 一视同仁**，根本不存在这个区别 —— 真正的问题是 TUN 没生效。

### 4. 移动目录会让 Python venv 失效

venv 里的脚本 shebang 和 `pyvenv.cfg` 写的是**绝对路径**。把 `agent-memory` 从 `MemorySystem/Codex` 搬到 `WorkPlan/` 之后，`.venv/bin/mypy` 直接报 `bad interpreter: .../MemorySystem/Codex/.venv/bin/python`。

**搬完目录先 `rm -rf .venv && uv sync --all-groups`**。venv 是可重建产物，不用心疼。

### 5. Windows 的 ssh 私钥必须收紧权限

OpenSSH 会拒绝加载权限过宽的私钥。放进 `~/.ssh/` 后必须：

```powershell
icacls $key /inheritance:r /grant:r "$($env:USERNAME):R"
```

### 6. `github.com` 这个 ssh 入口只能绑一把密钥

luowindows 上原本 `Host github.com` 绑的是 `id_rsa_newgithub`（shnh2001 账号），导致 ssh 认成 shnh2001。改成 luckyrichor 的密钥前**先扫一遍那台机器上还有没有别的账号的仓库**（本次扫描结果：零个 shnh2001 仓库，所以直接改默认最干净；老密钥保留为 `Host github-shnh2001` 别名备用）。

配置改动前备份：`~/.ssh/config.bak-before-luckyrichor`。

### 7. 读配置文件别用 `head` 截断

我第一次读 luowindows 的 `.ssh/config` 时用了 `head -40`，正好把末尾已有的 `Host github.com` 条目截掉了，于是误判「没有 github 配置」，后面才发现。**读配置文件要读全。**

### 8. tx 登录横幅会污染 ssh 输出

腾讯云会打印一大幅 ASCII 二维码（微信扫码登录提示）。批量跑命令时用 `sed -n '/=== MARK ===/,$p'` 之类的标记过滤，否则输出全是噪音，也容易让人误以为命令卡住了。

同样地，**PowerShell 脚本的输出是等脚本跑完才一次性返回的**，中间看起来像卡死其实只是在跑慢测试。

### 9. Testcontainers 需要 Docker daemon

`agent-memory` 的 `tests/api`、`tests/integration`、`tests/e2e` 走 Testcontainers。Docker 没起时会直接抛 `DockerException` 而**不是 skip**，看起来像代码坏了。先 `docker compose up -d postgres`。

---

## 四、换机器工作的固定动作

1. `git pull`（开工前）
2. 干活
3. `git push`（收工前，不留未推送的提交过夜）

**同一个项目同一时间只在一台机器上改。** 六个项目本就独立，不存在必须同机协作的场景。

新机器拉齐工作区：`bash workplan-docs/scripts/bootstrap-workspace.sh`
